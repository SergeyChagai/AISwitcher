# Research: выбор модели для Tier 1

Статус: **два кандидата замерены, один принят.** Этот документ — протокол Sprint 1;
по его итогам закрывается
[ADR-0002](../adr/ADR-0002-model-selection-for-context-disambiguation.md).

- Регистр текста — **отвергнут**: меняет precision на recall.
- Линейная модель над признаками Tier 0 — **принята**: P = 1.0000 при R = 0.6438.
- `multilingual-e5-small` — не замерен; переведён в источник признаков, см. таблицу кандидатов.

Цифры в разделе «Результаты» получены прогоном на нашем корпусе. Цифры в таблице «Кандидаты» —
ожидаемые порядки величин из открытых источников, а не измерения.

## Вопрос исследования

Даёт ли контекстная модель значимый прирост над детерминированным baseline на тех случаях, где
baseline проваливается (технический и смешанный текст, имена собственные, короткие токены), —
и укладывается ли она в бюджет фонового процесса?

Отрицательный ответ — валидный результат: продукт тогда выходит на одном Tier 0.

## Корпус

Нужен размеченный корпус: для каждого токена — требовалось ли переключение раскладки.

| Подкорпус | Что содержит | Зачем |
|---|---|---|
| `ru-clean` | обычный русский текст | базовое качество |
| `en-clean` | обычный английский текст | базовое качество |
| `mistyped-ru` | русский, набранный в EN-раскладке | очевидный положительный случай |
| `mistyped-en` | английский, набранный в RU-раскладке | очевидный положительный случай |
| `mixed-tech` | русский текст с вкраплениями `npm`, `git`, путей, идентификаторов | **ключевой подкорпус** |
| `terminal` | реальные последовательности команд | зона максимального вреда ложных срабатываний |
| `proper-nouns` | имена, бренды, транслит | внесловарные токены |
| `short` | токены длиной 1–3 символа | зона минимума статистики |

Источник `mixed-tech` и `terminal` можно собрать синтетически из публичных репозиториев и
истории команд, не привлекая личные данные пользователей.

## Метрики

Порядок приоритета отражает асимметрию стоимости ошибок из `docs/product/vision.md`.

1. **Precision переключений** — доля корректных среди всех сделанных переключений. Главная
   метрика. Цель: определить достижимый уровень, а не подгонять под красивое число.
2. **Recall при зафиксированном precision** — сколько реальных ошибок ловим, не выходя за
   допустимый уровень ложных.
3. **Precision на `mixed-tech` + `terminal` отдельно** — здесь baseline должен быть заметно
   хуже; если кандидат не выигрывает именно тут, он не нужен.
4. **p99-латентность одного вызова** — на целевом CPU, не на GPU.
5. **Размер артефакта и RSS процесса.**

## Кандидаты

| Кандидат | Роль | Ожидаемый размер | Главный риск |
|---|---|---|---|
| Карта раскладок + символьные n-граммы | **baseline**, Tier 0 | единицы МБ | слаб на внесловарных и коротких токенах |
| Линейная модель над признаками Tier 0 | **Tier 1, принята** | килобайты | мала выборка для обучения |
| `intfloat/multilingual-e5-small` | источник признаков, не решающий слой | ONNX fp32 ≈ 470 МБ, int8 существенно меньше | целевая функция ≠ задача; вес; не дообучается на пользователе (ADR-0005) |
| fastText LID | признак, не решающий слой | ~1 МБ (сжатый) | определяет язык корректного текста, не факт неверной раскладки |
| Малая causal LM (перплексия) | верхняя граница качества | сотни МБ+ | бюджет фонового процесса |

## Что конкретно проверить у e5

Гипотеза владельца проекта. Проверяется отдельно, потому что риск не в качестве модели, а в
её применимости:

- Как subword-токенизатор ведёт себя на входе вида `ghbdtn` — есть ли вообще устойчивое
  представление у последовательности, которой не было в обучении.
- Достаточно ли эмбеддинга контекста для бинарного решения, или поверх нужен обученный
  классификатор (и тогда сравнение честнее вести с «своей моделью под задачу»).
- Латентность int8-варианта на коротком входе на целевом CPU.
- Соблюдение протокола префиксов e5 (`query:` / `passage:`) — он часть контракта модели.

## Протокол

1. Зафиксировать корпус и разметку, положить в репозиторий (без персональных данных).
2. Прогнать baseline, получить опорные цифры.
3. Прогнать кандидатов на том же корпусе и том же железе.
4. Построить кривую precision/recall для каждого и выбрать рабочую точку по asymmetric cost.
5. Записать результаты в таблицу ниже и закрыть ADR-0002.

## Результаты

Замерены: baseline Tier 0, кандидат «регистр текста», кандидат «линейная модель».

Воспроизведение:

```
bash tools/build-corpus.sh
bash tools/fetch-opensubtitles.sh
cargo run --release -p eval
```

Без второго шага замер тоже пройдёт — просто без подкорпуса живой речи и с другими цифрами.

### Корпус, на котором получены цифры

| Подкорпус | Токенов | Источник |
|---|---|---|
| `ru` | 6 729 | кириллические токены из прозы собственной документации |
| `ru-speech` | 400 000 (потолок) | русские субтитры OPUS OpenSubtitles v2018 |
| `en` | 67 387 | латинские токены из прозы собственной документации |
| `tech` | 13 860 | латинские токены из кода и inline-code той же документации |
| `terminal` | ~290 | `corpus/terminal/commands.txt`, написан вручную |
| `context` | 105 предложений, 612 токенов | `corpus/context/`, размечен вручную по семействам A–H |

`ru-speech` не хранится в репозитории: это чужой текст под своей лицензией. Он скачивается
`tools/fetch-opensubtitles.sh` диапазонным запросом первых мегабайт — полный `ru.txt.gz`
весит 655 МБ, а модель насыщается на порядки меньшем объёме.

Сплит — **по типам, а не по вхождениям**: каждый пятый уникальный токен уходит в тест вместе
со всеми вхождениями, обучение его не видит. Сплит по вхождениям давал утечку (частотное слово
попадало в обе части) и завышал recall примерно на 1,5 пункта.

### Baseline: карта раскладок + символьные 4-граммы

Рабочая точка `margin = 0.7`, выбрана по развёртке ниже.

| Метрика | Значение |
|---|---|
| Precision (switch) | **0.9982** |
| Recall (switch) | **0.9731** |
| TP / FP / FN / TN | 9593 / 17 / 265 / 10394 |
| Латентность p50 | 12.0 мкс |
| Латентность p99 | 29.2 мкс |
| Размер модели | счётчики n-грамм в памяти, единицы МБ |
| Тестовых случаев | 20 269 |

По подкорпусам:

| Подкорпус | Случаев | Ошибок | Доля верных |
|---|---|---|---|
| `en-clean` | 971 | 1 | 0.9990 |
| `ru-clean` | 508 | 1 | 0.9980 |
| `ru-speech` | 8 498 | 13 | 0.9985 |
| `mistyped-en` | 971 | 38 | 0.9609 |
| `mistyped-ru` | 508 | 7 | 0.9862 |
| `mistyped-speech` | 8 379 | 220 | 0.9737 |
| `mixed-tech` | 398 | 2 | 0.9950 |
| `terminal` | 36 | 0 | 1.0000 |

Развёртка по порогу:

| margin | Precision | Recall | FP | FN |
|---|---|---|---|---|
| 0.0 | 0.9901 | 0.9912 | 98 | 87 |
| 0.3 | 0.9950 | 0.9853 | 49 | 145 |
| 0.5 | 0.9971 | 0.9797 | 28 | 200 |
| **0.7** | **0.9982** | **0.9731** | **17** | 265 |
| 1.0 | 0.9992 | 0.9608 | 8 | 386 |
| 1.2 | 0.9994 | 0.9498 | 6 | 495 |

Кривая продолжает расти после 0.7, в отличие от прогона без живой речи. Рабочая точка
подлежит пересмотру, когда будет чем измерить реальную цену ложного срабатывания:
`margin = 1.0` убирает больше половины оставшихся FP ценой 1.2 пункта recall.

### Что изменилось после подключения живой речи

До `ru-speech` русская часть корпуса состояла из письменной технической прозы — 6.7k токенов
против 67k английских. Подключение субтитров (`tools/fetch-opensubtitles.sh`) увеличило
тест-сет с 3 392 до 20 269 случаев и сдвинуло картину:

- **RU-модель окрепла.** `ru-speech` как keep — 13 ошибок на 8 498 случаев. Разговорная
  лексика опознаётся не хуже письменной.
- **`mistyped-en` просел с 0.9743 до 0.9609.** Это прямое следствие: чем лучше RU-модель
  объясняет кириллическую последовательность, тем охотнее она принимает за русский текст
  «мусор», полученный из английского слова, и переключение не срабатывает. Обмен ожидаемый,
  но его стоит держать в виду.
- **Короткие токены стали слабее: 148 ошибок из 707** (21%) против 30 из 265 (11%). Живая речь
  состоит из коротких слов гораздо в большей степени, чем документация, поэтому эта зона
  наконец представлена честно. **Здесь и находится вся практическая ценность Tier 1.**

### Контекстный подкорпус: чему равен бюджет Tier 1

612 токенов, 105 предложений, разметка ручная. Та же модель Tier 0, без изменений.

| Метрика | Значение |
|---|---|
| Precision | 0.9608 |
| Recall | 0.6712 |
| Ложных срабатываний | **2** |
| Пропусков | 24 |

| Семейство | Токенов | FP | FN | Доля верных |
|---|---|---|---|---|
| A — корректная техническая вставка | 64 | 0 | 0 | 1.0000 |
| B — вставка в RU-раскладке | 61 | 0 | 0 | 1.0000 |
| C — русский целиком в EN-раскладке | 32 | 0 | 0 | 1.0000 |
| D — английский текст | 55 | 0 | 0 | 1.0000 |
| **E — неоднозначные короткие токены** | 199 | **2** | 16 | 0.9095 |
| F — терминальные команды | 33 | 0 | 0 | 1.0000 |
| G — имена собственные | 44 | 0 | 0 | 1.0000 |
| **H — короткие бренды и регистр** | 124 | 0 | 8 | 0.9355 |

Семейства A, F и G — корректные технические вставки, терминальные команды и имена
собственные — по-прежнему проходятся без единой ошибки. Именно на них жалуются пользователи
классических переключателей.

Все ошибки собраны в E и H, то есть там, где решение без окружения невозможно в принципе.

#### Два ложных срабатывания

Первая версия подкорпуса давала ноль ложных срабатываний. После расширения E и H появились
два — и оба реалистичны:

| Предложение | Токен | Что сделал |
|---|---|---|
| Гы это и правда смешно вышло | `Гы` | переключил в `us` |
| Купил два кг яблок на рынке | `кг` | переключил в `ru` |

Механика в обоих случаях одна: русское сокращение или разговорное словечко редко в корпусе,
а его латинский двойник (`us`, `ru`) — частотный технический токен. Модель выбирает частотное.
Это ровно та ошибка, из-за которой выключают Punto Switcher.

Вывод не в том, что baseline стал хуже. Он не менялся — **изменился корпус, и прежний ноль был
свойством лёгкого корпуса, а не свойством детектора.**

#### Что продукт делает сегодня, без Tier 1

Раз решения поднимаются наверх, а наверху пока пусто, есть две возможные политики. Обе
измерены:

| Политика | Precision | Recall | FP | FN |
|---|---|---|---|---|
| Tier 0 решает сам | 0.9608 | 0.6712 | 2 | 24 |
| Поднятое наверх не переключать | **1.0000** | 0.3973 | **0** | 44 |

Вторая строка — буквальное соблюдение принципа «при неуверенности молчать» из ADR-0001. Она
даёт идеальную precision ценой того, что продукт пропускает **шесть ошибок из десяти**.

Это и есть цена отсутствия Tier 1, выраженная в числах. Ни одна из двух политик не является
приемлемым продуктом: первая переключает то, что трогать нельзя, вторая почти не работает.
### Признак соседства: дешёвый контекст без модели

Раскладку забывают не на одно слово. Если соседний токен уже уверенно опознан как набранный
не в той раскладке, текущий почти наверняка из той же серии — значит, порог для него можно
ослабить. Это единственный контекстный признак, не требующий ни модели, ни словаря.

Результат: **12 ошибок → 11, ложные срабатывания остались на нуле.** Признак закрыл `пше`
(`git` в «Сделай `{пше}` `{згыр}`»), и семейство B стало чистым.

И он **не закрыл ни одной ошибки семейств E и H** — что было предсказуемо, но проверить
стоило. Причина в том, что там неверный токен окружён корректным русским текстом:

| Предложение | Токен | Ожидание |
|---|---|---|
| Вы уже посмотрели этот документ | `вы` | keep |
| Переменная `вы` хранит выборку | `вы` | switch |

Соседи в обоих случаях — обычные русские слова. Признак «язык окружения» их не различает
вовсе, потому что окружение идентично. Различает только смысл: «переменная … хранит выборку»
говорит об имени переменной. **Дешёвым контекстным признаком эти случаи не берутся** — и это
прямой аргумент в пользу того, что Tier 1 нужен, а не является перестраховкой.

### Правило вызова Tier 1: цена против охвата

Решение поднимается на Tier 1, когда отрыв гипотез попадает в полосу неуверенности шириной
`band` вокруг порога, либо когда токен непригоден для n-граммной статистики (капслок или
длина ≤3) и при этом альтернатива не отвергнута уверенно.

Оговорка про «не отвергнута уверенно» существенна: русский текст состоит из коротких слов
(«не», «на», «что»), и наивное правило «длина ≤3 → наверх» отправляло бы на Tier 1 треть всех
решений впустую.

| `band` | Поднято наверх | Доля решений | Ошибок доступно модели |
|---|---|---|---|
| 0.25 | 38 | 6.2% | 16 из 27 |
| 0.50 | 42 | 6.9% | 18 из 27 |
| 1.00 | 74 | 12.1% | 20 из 27 |
| **2.00** | **145** | **23.7%** | **26 из 27** |
| 3.00 | 228 | 37.3% | 27 из 27 |
| 5.00 | 535 | 87.4% | 27 из 27 |

Кривая ставит выбор ребром. При узкой полосе Tier 1 дёшев, но получает меньше половины
ошибок — то есть не окупается ни при каком качестве модели. Чтобы дать ему шанс на 26 из 27,
приходится поднимать наверх каждое пятое решение.

Принято `band = 2.0`. Оценка стоимости: при темпе набора порядка 3 токенов в секунду это
примерно 0.7 вызова модели в секунду; при бюджете Tier 1 в десятки миллисекунд — единицы
процентов одного ядра. Приемлемо, но это уже не «модель вызывается редко», и в ADR-0001 такая
формулировка требует уточнения по итогам спринта.

### Регистр отдельно: что он чинит, а что нет

Четыре ошибки семейства H (`VUE`, `DRC`, `<L`) — токены в верхнем регистре. Соблазнительно
считать, что признак регистра их закроет, но это не так:

- Признак «заглавная не в начале фразы → keep» работает в **обратную** сторону: он усилил бы
  и без того неверное решение не переключать.
- Признак «капслок → аббревиатура» верен, но аббревиатуры есть в обеих раскладках
  (`API` против `ВКС`), поэтому сам по себе он не выбирает между ними.

Проку от регистра здесь ровно один: он даёт понять, что n-граммная статистика к токену
неприменима — аббревиатура не подчиняется буквосочетаемости языка. Это основание **снять
решение с Tier 0 и передать его выше**, а не решить на месте. То есть регистр — не замена
Tier 1, а критерий его вызова.

Для `Vue` против `VUE` иного пути и нет: различить фреймворк и университет можно только по
окружению («переписали фронт на …» против «поступил в …»).

### Кандидат Tier 1 №1: регистр текста — отвергнут

Первый кандидат выбран по принципу «самое дешёвое вперёд» и не является моделью вовсе.

Наблюдение, из которого он вырос: почти все ошибки различаются не языком окружения — оно
везде русское, — а **регистром текста**.

| Предложение | Верное чтение |
|---|---|
| Свет луны падал прямо на воду | `луны` — русское слово |
| Все `луны` лежат в переменных окружения | `keys` — набрано не в той раскладке |

«Переменных окружения» — технический русский, «свет падал на воду» — бытовой. Оба словаря уже
собраны: `corpus/ru` (собственная документация) против `corpus/ru-speech` (субтитры). Признак
считается как логарифм отношения частот, вызывается только на поднятых решениях и сдвигает
порог: технический контекст удешевляет переключение кириллицы в латиницу, бытовой — удорожает.

Реализация: `crates/eval/src/register.rs`. Ни модели, ни обучения, две хэш-таблицы.

| вес | Precision | Recall | FP | FN |
|---|---|---|---|---|
| 0.00 (выключен) | **0.9600** | 0.6575 | **2** | 25 |
| 0.25 | 0.9245 | 0.6712 | 4 | 24 |
| 0.50 | 0.9273 | 0.6986 | 4 | 22 |
| 1.00 | 0.8966 | 0.7123 | 6 | 21 |
| 2.00 | 0.7794 | 0.7260 | 15 | 20 |
| 4.00 | 0.6790 | 0.7534 | 26 | 18 |

**Отвергнут.** Ни при каком весе признак не улучшает precision — он только меняет её на recall.
Лучшая точка (вес 0.5) покупает три пропуска ценой двух новых ложных срабатываний, и ни одно
из двух исходных не исправляет. При асимметричной стоимости ошибок обмен идёт в неверную
сторону по определению.

#### Ошибка в самом замере, найденная по дороге

Первый прогон дал precision 0.58 при весе 4 — результат выглядел катастрофой, но был артефактом
конструкции. Технический корпус на два порядка меньше бытового (5 416 токенов против 337 326),
поэтому слово, встретившееся в документации **один раз**, получало оценку «в шестьдесят раз
характернее для технического текста». Арифметически верно, статистически бессмысленно: признак
мерил не регистр, а «попадалось ли слово в нашем маленьком корпусе».

Исправлено усадкой по объёму свидетельств — вес слова умножается на `n / (n + 10)`. Цифры выше
получены уже после исправления. Вывод от этого не изменился, но без исправления замер
не имел бы силы.

#### Почему это не приговор идее

Признак недоизмерен, а не опровергнут:

- Технический русский словарь — **2 036 слов**, и это словарь *архитектурной документации*:
  «решение», «модель», «слой». Слов, которыми разработчик говорит в переписке, — «запусти»,
  «папку», «базе», «переменных» — в нём нет вовсе.
- Покрытие контекста 64.8%, но покрыты в основном общеупотребительные русские слова, у которых
  никакого регистрового сигнала нет.

Чтобы проверить идею по-настоящему, нужен корпус **технической русской речи**, а не технической
документации: комментарии профильных площадок, обсуждения в трекерах. Такого источника у нас
пока нет — это тот самый пробел, который отмечен в ограничениях выше.

### Кандидат Tier 1 №2: линейная модель над признаками Tier 0 — принят

Единственный кандидат из списка, чья целевая функция совпадает с задачей: он обучается прямо
на бинарном решении «переключать или нет», а не оценивает правдоподобие текста в надежде, что
разность оценок окажется полезной.

Форма выбрана под требование [ADR-0005](../adr/ADR-0005-personalization-and-online-learning.md):
девять весов, обновление одним шагом градиента, интерпретируемость. Признаки берутся у Tier 0,
который их уже посчитал, поэтому вызов почти ничего не стоит.

Реализация: `crates/eval/src/linear.rs`. Оценка — перекрёстная проверка с исключением **по
одному предложению**, 105 фолдов. Случайный сплит по токенам дал бы утечку: предложения идут
парами (одно чтение токена в `keep`, другое в `switch`), и разнести пару по разные стороны
значит показать модели ответ.

| порог | Precision | Recall | FP | FN |
|---|---|---|---|---|
| 0.30 | 0.8451 | 0.8219 | 11 | 13 |
| 0.50 | 0.9273 | 0.6986 | 4 | 22 |
| **0.70** | **1.0000** | **0.6438** | **0** | 26 |
| 0.90 | 1.0000 | 0.5205 | 0 | 35 |

Сравнение с политиками без Tier 1, на том же корпусе:

| Политика | Precision | Recall | FP | FN |
|---|---|---|---|---|
| Tier 0 решает сам | 0.9608 | 0.6712 | **2** | 24 |
| Поднятое наверх не переключать | 1.0000 | 0.3973 | 0 | 44 |
| **Линейная модель, порог 0.70** | **1.0000** | **0.6438** | **0** | 26 |

**Принят.** Модель достигает идеальной precision консервативной политики, сохраняя при этом
в полтора раза больше recall (0.6438 против 0.3973). Оба ложных срабатывания — `Гы`→`us` и
`кг`→`ru` — устранены. Цена: два пропуска сверх исходных 24.

Это первый кандидат, который покупает precision, а не меняет её на recall.

#### Чему модель научилась

Веса читаемы, и это часть требования ADR-0005:

| Признак | Вес |
|---|---|
| отрыв гипотез | +3.173 |
| отрыв минус порог | +2.766 |
| капслок | +1.899 |
| смещение | −1.259 |
| сосед переключён | +1.138 |
| короткий (≤3) | +0.434 |
| регистр контекста | +0.346 |
| длина токена | −0.345 |
| заглавная в середине | +0.070 |

Два наблюдения:

- **Капслок получил третий по величине вес.** Признак регистра, который сам по себе ничего не
  чинил (раздел выше), в сочетании с остальными оказался сильным. Порознь эти признаки
  бесполезны, вместе — работают: ровно то, ради чего берут обучаемую модель вместо набора
  правил.
- **Регистр контекста выжил с весом +0.346** — отвергнутый кандидат №1 остался слабым
  признаком. Отвергать его как *решающий слой* было верно; выбрасывать совсем — нет.

#### Ограничения

- **612 токенов, 105 предложений.** Перекрёстная проверка смягчает, но не устраняет: девять
  весов на такой выборке — это на грани. Цифры показывают направление, а не точное качество.
- **Корпус написан мной**, семейства сбалансированы искусственно. Доля `switch` в нём куда выше
  реальной.
- **Recall 0.6438 — это всё ещё треть пропусков.** В семействе E остаётся 16 ошибок: коллизии
  вида `руку`/`here` не берутся ни одним из проверенных признаков.

### Третий исход: спросить пользователя

Часть коллизий неразрешима в принципе: `руку`/`here`, `Vue`/`МГУ`, `ТЗ`/`NP` — один набор
нажатий, два осмысленных прочтения, и если человек употребляет оба, выучить его предпочтение
нельзя, потому что предпочтения нет. Детектор при этом обязан молча выбрать и в трети случаев
выбирает неверно.

Вопрос задаётся, когда вероятность по Tier 1 попала в узкую полосу вокруг 0.5 — то есть модель
не склоняется ни к чему. Это строго уже зоны эскалации: большинство поднятых наверх решений
Tier 1 разрешает уверенно.

| полоса вокруг 0.5 | вопросов | доля токенов | ошибок снято | ошибок осталось |
|---|---|---|---|---|
| ±0.05 | 6 | 1.0% | 3 | 23 |
| **±0.10** | 8 | **1.3%** | 5 | 21 |
| ±0.15 | 13 | 2.1% | 7 | 19 |
| ±0.20 | 24 | 3.9% | **13** | 13 |
| ±0.30 | 37 | 6.0% | 17 | 9 |

Соотношение выгодное: на ±0.20 снимается **половина всех ошибок** ценой вопроса на каждый
25-й токен. Наверх при этом поднимается 23.7% решений — спрашивать приходится в шесть раз реже,
чем звать Tier 1.

Решение и ограничения — [ADR-0006](../adr/ADR-0006-ask-the-user-as-a-third-outcome.md).

Оговорка, без которой таблица вводит в заблуждение: **цена прерывания здесь не учтена вовсе.**
«Ошибок снято» считается в предположении, что человек отвечает и отвечает верно. Сколько стоит
сам вопрос — сколько их в минуту терпимо, сколько остаются без ответа — измерить на корпусе
нельзя. Поэтому стартовая полоса выбрана ±0.10, не ±0.20: не потому что лучше по цифрам, а
потому что начинать надо с осторожной стороны.

### Что из этого следует

1. **Латентность не является проблемой.** 29.2 мкс на p99 — три порядка запаса до
   миллисекундного бюджета ADR-0001. Планка для Tier 1 определяется не скоростью, а качеством.
2. **Планка для Tier 1 двойная.** На потокенном прогоне baseline даёт 17 ложных срабатываний
   на 20 269 решений, на контекстном — 2 на 612. Кандидат должен убрать эти два и закрыть
   пропуски, не тронув то, что уже работает.
3. **Остаток сосредоточен в коротких токенах.** 148 ошибок из 707 случаев длиной ≤3 символа —
   это 21% против 1.4% в среднем. Здесь статистики почти нет, и именно здесь контекст
   потенциально что-то даёт.
4. **`mixed-tech` baseline не проваливает** — 2 ошибки из 398, а семейства A, F и G
   контекстного подкорпуса проходятся начисто. Исходное ожидание «сломается на техническом
   тексте» не подтвердилось: ломается он не на технической вставке как таковой, а на
   коллизиях, где обе раскладки дают осмысленный токен.

### Ограничения этих цифр

Читать результаты без этого раздела нельзя.

- **Главное: тест не проверяет тот случай, ради которого задуман Tier 1.** Токены оцениваются
  поодиночке, вне предложения. Сценарий из `docs/product/vision.md` — `npm` внутри русской
  фразы, где решение требует окружения — в корпусе отсутствует. `mixed-tech` сейчас проверяет
  лишь то, что технический токен опознаётся как латиница, а это baseline умеет. **Пока такого
  подкорпуса нет, сравнивать Tier 1 с baseline бессмысленно: мерить будет нечего.**
- **Живая речь есть, живой переписки нет.** Субтитры дают разговорную лексику и короткие
  реплики, но не дают смешанного технического русского — того самого жанра, где продукт
  ломается. Ближайший подходящий источник такого текста — комментарии технических площадок.
- **Эталон выведен автоматически**, а не размечен человеком: корректный токен → `keep`, его
  транслитерация → `switch`. Это исключает ошибки разметки, но и исключает случаи, где верное
  решение неочевидно.
- **Тест по-прежнему потокенный.** Контекстный подкорпус (`corpus/context/`) собран, но
  харнесс его пока не читает: замер идёт по отдельным токенам. Семейства A-H задействуются
  на следующем шаге.
- **Подкорпус `terminal` мал** (36 тестовых случаев). Его 100% ничего не доказывают.

### Регистр как потерянный сигнал

Отдельная находка вычитки контекстного подкорпуса: **baseline не видит регистр вообще**.
Токенизатор в `crates/ngram` приводит всё к нижнему, поэтому `Vue` и `VUE` для модели
неразличимы — а это разные решения:

| Набрано | Что имелось в виду | Решение |
|---|---|---|
| `Vue` | фреймворк | keep |
| `VUE` | `МГУ` | switch |

Нажатия одни и те же, отличается только регистр. То же и с заглавной буквой в середине
предложения: `Go`, `Nx`, `Qt` — почти наверняка имя собственное, то есть сигнал в пользу
`keep`, который сейчас выбрасывается.

Из этого следуют два признака Tier 0, которых пока нет:

- заглавная буква не в начале предложения → вес в пользу `keep`;
- полностью заглавный короткий токен → аббревиатура, но в **обеих** раскладках
  (`API` против `ВКС`), поэтому признак работает только вместе с окружением.

Признак дешёвый и детерминированный, то есть его место в Tier 0, а не в модели. Проверять
его нужно на семействе H контекстного подкорпуса.

### Следующий шаг

Мерить кандидата Tier 1 теперь есть на чём, и критерий приёмки сформулирован в числах:

> Закрыть как можно больше из 26 ошибок контекстного подкорпуса, **убрав два ложных
> срабатывания и не добавив новых**, и не ухудшив общий прогон
> (P = 0.9982 на 20 269 случаях).

Порядок работ:

1. Ввести правило вызова Tier 1: решение поднимается выше, когда отрыв гипотез мал либо когда
   токен непригоден для n-граммной статистики (капслок, длина ≤3). Замерить, какая доля
   решений при этом уходит наверх — это прямая цена по CPU.
2. Только после этого — кандидаты. Первым идёт не e5, а самый дешёвый вариант: контекстный
   признак «язык предыдущих токенов». Если 12 случаев закрываются им, ADR-0002 закрывается
   без модели вообще.
3. `multilingual-e5-small` проверяется последним, потому что дороже всех и потому что открытые
   вопросы ADR-0002 к нему остаются в силе.

Отдельно: контекстный подкорпус в 105 предложений всё ещё мал для выводов о десятых долях
процента, но расширение E и H уже окупилось — оно вскрыло два ложных срабатывания, которых
на прежнем корпусе не было видно.

## Источники

- [Punto Switcher — Grokipedia](https://grokipedia.com/page/Punto_Switcher) — n-граммный и
  словарный принцип классических переключателей
- [intfloat/multilingual-e5-small — Hugging Face](https://huggingface.co/intfloat/multilingual-e5-small)
  — 12 слоёв, размерность 384, ONNX в fp32/int8/uint8
- [How to compute LLM embeddings 3X faster with model quantization](https://medium.com/nixiesearch/how-to-compute-llm-embeddings-3x-faster-with-model-quantization-25523d9b4ce5)
  — порядок ускорения от квантования на коротких входах
